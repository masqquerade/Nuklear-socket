#include <WinSock2.h>
#include <Windows.h>
#include "Protocol.h"
#include "IOController.h"

// TODO

bool Protocol::ProtoRecv(SOCKET fd, char *buf)
{
	return true;
}

bool Protocol::ProtoSend(SOCKET fd, char* buf)
{
	return true;
}